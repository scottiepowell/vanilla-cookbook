name: CI - Test and Build (Latest)

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  run-tests:
    if: (github.repository == 'jt196/vanilla-cookbook' || github.repository == 'scottiepowell/vanilla-cookbook') && github.event_name == 'pull_request'
    uses: ./.github/workflows/run-tests.yml
    with:
      run_e2e: false
      run_llm_smoke: false
    secrets:
      OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
      ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
      GOOGLE_API_KEY: ${{ secrets.GOOGLE_API_KEY }}
      OLLAMA_BASE_URL: ${{ secrets.OLLAMA_BASE_URL }}

  docker-build-latest:
    if: github.repository == 'jt196/vanilla-cookbook' && github.event_name == 'push'
    uses: ./.github/workflows/docker-build.yml
    with:
      docker_tags: latest
    secrets:
      DOCKER_HUB_USERNAME: ${{ secrets.DOCKER_HUB_USERNAME }}
      DOCKER_HUB_PASSWORD: ${{ secrets.DOCKER_HUB_PASSWORD }}

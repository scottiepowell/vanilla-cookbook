name: Run Tests

on:
  workflow_call:
    inputs:
      run_e2e:
        required: false
        type: boolean
        default: false
      run_llm_smoke:
        required: false
        type: boolean
        default: false
    secrets:
      OPENAI_API_KEY:
        required: false
      ANTHROPIC_API_KEY:
        required: false
      GOOGLE_API_KEY:
        required: false
      OLLAMA_BASE_URL:
        required: false

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      DATABASE_URL: file:./prisma/db/ci-test.sqlite
      OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
      ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
      GOOGLE_API_KEY: ${{ secrets.GOOGLE_API_KEY }}
      OLLAMA_BASE_URL: ${{ secrets.OLLAMA_BASE_URL }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          submodules: 'recursive'

      - name: Install pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install

      - name: Install Playwright Browsers
        if: ${{ inputs.run_e2e }}
        run: pnpm exec playwright install --with-deps chromium firefox webkit

      - name: Set environment variables
        run: |
          echo "ORIGIN=http://127.0.0.1:3000" >> .env
          echo "DATABASE_URL=file:./prisma/db/ci-test.sqlite" >> .env
          echo "ADMIN_USER=admin" >> .env
          echo "ADMIN_PASSWORD=password" >> .env
          echo "ADMIN_EMAIL=email@example.com" >> .env
          echo "ADMIN_NAME=Administrator" >> .env
          echo "OPENAI_API_KEY=dummy_key" >> .env
          echo "LLM_API_ENGINE_TEXT=gpt-3.5-turbo" >> .env
          echo "LLM_API_ENGINE_IMAGE=gpt-4o" >> .env
          echo "LLM_API_ENABLED=false" >> .env

      - name: Create Directories
        run: |
          mkdir -p prisma/db
          mkdir -p app/uploads/imports
          mkdir -p app/uploads/images

      - name: Build Project
        run: pnpm build

      - name: Run Prisma Migrations
        run: pnpm prisma migrate deploy

      - name: Seed ingredient data
        run: pnpm seed

      - name: Start Node Server
        run: node build/index.js > server.log 2>&1 &
        env:
          PORT: 3000
          ORIGIN: http://127.0.0.1:3000
          NODE_ENV: production

      - name: Wait for Health (max 90s)
        run: |
          echo "Waiting for /api/health..."
          for i in {1..90}; do
            if curl -sSf -o /dev/null http://127.0.0.1:3000/api/health; then
              echo "Server is healthy"
              exit 0
            fi
            sleep 1
          done
          echo "::error::Health check failed. Logs:"
          sed -n '1,200p' server.log || true
          exit 1

      # Seed exactly once (remove if your app seeds itself at startup)
      - name: Seed the Database
        run: |
          curl -sS -X POST \
            -H "Content-Type: application/json" \
            -d '{
                  "adminUser": {
                    "adminName": "Administrator",
                    "adminUsername": "admin",
                    "adminEmail": "email@example.com",
                    "adminPassword": "password",
                    "adminUnits": "metric",
                    "adminLanguage": "eng",
                    "recipeSeed": true
                  }
                }' \
            http://127.0.0.1:3000/api/site/seed

      - name: Run Tests
        run: pnpm test

      - name: Run LLM Smoke Tests (optional)
        if: ${{ inputs.run_llm_smoke && (env.OPENAI_API_KEY != '' || env.ANTHROPIC_API_KEY != '' || env.GOOGLE_API_KEY != '' || env.OLLAMA_BASE_URL != '') }}
        continue-on-error: true
        run: RUN_LLM_SMOKE=true pnpm -s vitest run src/tests/llm.smoke.test.js

      - name: Run Playwright E2E
        if: ${{ inputs.run_e2e }}
        run: pnpm exec playwright test
        env:
          # Build already ran earlier; skip to avoid duplicate work in the E2E webServer script.
          E2E_SKIP_BUILD: '1'
